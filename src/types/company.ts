export type CompanySchedule = {
  workDays: number[];
  openingTime: string;
  closingTime: string;
};

export type Staff = {
  id: string;
  name: string;
  color: string;
  role: string;
  image: string;
};
